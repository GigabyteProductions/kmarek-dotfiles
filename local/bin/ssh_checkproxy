#!/bin/bash

if [[ ${SSH_SKIP_PROXY,,} =~ ^(yes|true|1)$ ]]; then
	exit 1
fi

domain="$1"

if [ -e "/var/run/user/$UID/ssh/skip_proxy.d/$domain" ]; then
	exit 1
fi

case "$domain" in
	*)
		echo "$0: No test for domain: $domain" >&2
		exit 2
	;;
esac
