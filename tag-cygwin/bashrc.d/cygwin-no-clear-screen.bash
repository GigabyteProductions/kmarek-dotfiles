# cygwin-no-clear-screen.bash

# to combat these lines from Cygwin's /etc/bash.bash_logout:
# # when leaving the console clear the screen to increase privacy
# if [ "$SHLVL" = 1 ]; then
#   [ -x /usr/bin/clear ] && /usr/bin/clear
# fi
SHLVL=0
